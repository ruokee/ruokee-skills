interface:
  display_name: "Task"
  short_description: "Create, resume, and manage persistent project Tasks"
  default_prompt: "Use $task to resume and continue an existing Task in the current project."
