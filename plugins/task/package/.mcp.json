{
  "mcpServers": {
    "task": {
      "command": "sh",
      "args": [
        "-c",
        "if [ -n \"${CLAUDE_PLUGIN_ROOT:-}\" ]; then plugin_root=$CLAUDE_PLUGIN_ROOT; export TASK_HOST=claude; else plugin_root=.; export TASK_HOST=codex; fi; exec \"$plugin_root/bin/task-core\" mcp"
      ],
      "cwd": "."
    }
  }
}
